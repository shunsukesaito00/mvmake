# v1.14.0

トランスフォームキーフレーム（位置・スケール・回転）MVP リリースです。クリップの位置変化を時間軸でアニメーションできます。

デモ: https://shunsukesaito00.github.io/mvmake/

## 追加

- **トランスフォームキーフレーム (MVP)**: 動画/画像/テキストクリップで X・Y・スケール・回転をキーフレーム補間
- **インスペクター UI**: 「トランスフォームキーフレーム」セクションで追加・編集・削除
- **タイムライン表示**: クリップ上端に菱形マーカー。ドラッグで時間移動

## 改善

- **compositor**: `getTransformAtLocalTime` でプレビュー・書き出し両方に補間 transform を反映
- **E2E**: 全 51 シナリオ（トランスフォームキーフレーム追加を追加）
- **ユニットテスト**: 223 件（`transformKeyframes` を追加）

## 運用上の注意

- ImageClip で Ken Burns 有効時は transform キーフレームを無効化（Ken Burns を優先）
- main への **連続 push は避け**、1 回の push ごとに Deploy ワークフローの完了（build → deploy → smoke）を待ってから次を push してください

---

# v1.13.0

BGM/ナレーション音量正規化 MVP リリースです。素材のピークに合わせてクリップ音量を自動調整できます。

デモ: https://shunsukesaito00.github.io/mvmake/

## 追加

- **音量正規化 (MVP)**: オーディオ/動画クリップのインスペクターから「音量を正規化」。素材ピークを約 90% に合わせる
- **キーフレーム連動**: 既存の `volume` と `volumeKeyframes` を同倍率でスケール。プレビュー・書き出し両方に反映

## 改善

- **ユーティリティ**: `audioNormalize.ts` でピーク測定・倍率計算を共通化
- **E2E**: 全 50 シナリオ（音量正規化フローを追加）
- **ユニットテスト**: 219 件（`audioNormalize` を追加）

## 運用上の注意

- main への **連続 push は避け**、1 回の push ごとに Deploy ワークフローの完了（build → deploy → smoke）を待ってから次を push してください

---

# v1.12.0

プロジェクト設定プリセットのユーザー保存 MVP リリースです。横型/縦型などの制作フォーマットを名前付きで切り替えられます。

デモ: https://shunsukesaito00.github.io/mvmake/

## 追加

- **設定プリセット保存 (MVP)**: 解像度・FPS・リップル削除・ループ再生をセットで localStorage に保存
- **設定プリセット適用**: プロジェクト設定モーダルからワンクリックで切替

## 改善

- **ストア統合**: `applyProjectSettingsPreset` で解像度と編集設定を一括適用（Undo 対応）
- **E2E**: 全 49 シナリオ（設定プリセット保存・適用を追加）
- **ユニットテスト**: 213 件（`projectSettingsPresetUtils`・ストア適用を追加）

## 運用上の注意

- main への **連続 push は避け**、1 回の push ごとに Deploy ワークフローの完了（build → deploy → smoke）を待ってから次を push してください

---

# v1.11.0

タイムラインズーム・ナビゲーション改善リリースです。長尺婚礼ムービーの編集で、選択クリップへのズームと再生位置の追従がしやすくなりました。

デモ: https://shunsukesaito00.github.io/mvmake/

## 追加

- **選択クリップへズーム (Z)**: 選択中クリップを画面中央付近に拡大表示
- **再生位置を中央へ**: ボタンで再生ヘッドをタイムライン中央へスクロール
- **再生中の自動追従**: 再生中は再生ヘッドが常に中央付近を追従

## 改善

- **ズーム倍率の保持**: `pixelsPerSecond` を localStorage に保存し、次回起動時も維持
- **フィット計算の共通化**: `timelineZoom` ユーティリティでフィット/ズームロジックを整理
- **E2E**: 全 48 シナリオ（タイムラインズーム回帰を追加）
- **ユニットテスト**: 206 件（`timelineZoom` を追加）

## 運用上の注意

- main への **連続 push は避け**、1 回の push ごとに Deploy ワークフローの完了（build → deploy → smoke）を待ってから次を push してください

---

# v1.10.0

ユーザーテンプレートのエクスポート/インポート MVP リリースです。保存済みテンプレートを JSON ファイルで共有・バックアップできます。

デモ: https://shunsukesaito00.github.io/mvmake/

## 追加

- **テンプレートのエクスポート**: `.fable-template.json` 形式でダウンロード
- **テンプレートのインポート**: ファイルから一覧へ追加（`label` 重複時は `(インポート)` を付与）
- **スキーマバージョン**: `schemaVersion: 1` で将来の互換管理

## 改善

- **ProjectListModal 連携**: テンプレートから新規作成セクションでもインポート可能
- **E2E**: 全 47 シナリオ（エクスポート→localStorage クリア→インポート→新規作成を追加）
- **ユニットテスト**: 200 件（`userProjectTemplateExport` を追加）

## 運用上の注意

- main への **連続 push は避け**、1 回の push ごとに Deploy ワークフローの完了（build → deploy → smoke）を待ってから次を push してください

---

# v1.9.0

テンプレートのユーザー保存 MVP リリースです。よく使うプロジェクト構成を名前付きで保存し、新規プロジェクト作成やテンプレタブから再利用できます。

デモ: https://shunsukesaito00.github.io/mvmake/

## 追加

- **ユーザーテンプレート保存 (MVP)**: トラック配置・クリップ・マーカー・解像度/FPS を localStorage に保存
- **テンプレートから新規作成**: プロジェクト一覧モーダルから保存済みテンプレートで新規プロジェクトを作成
- **テンプレタブでの適用**: マイテンプレート一覧から現在のプロジェクトへ構成を適用

## 改善

- **メディア非依存の再利用**: メディア素材は含めず配置情報のみ復元（素材は後から追加可能）
- **E2E**: 全 46 シナリオ（ユーザーテンプレート保存・新規作成・適用を追加）
- **ユニットテスト**: 192 件（`userProjectTemplate`・ストア適用を追加）

## 運用上の注意

- main への **連続 push は避け**、1 回の push ごとに Deploy ワークフローの完了（build → deploy → smoke）を待ってから次を push してください

---

# v1.8.0

書き出し UX 改善リリースです。長時間の書き出しでも進捗・残り時間・エラー・キャンセル後の操作が分かりやすくなりました。

デモ: https://shunsukesaito00.github.io/mvmake/

## 追加

- **残り時間推定**: 書き出し進捗に ETA（「残り約 N分M秒」）を表示
- **詳細エラー表示**: 失敗時にモーダル内へエラー詳細と「設定に戻る」ボタンを表示
- **キャンセル後の状態整理**: キャンセル後もモーダル内で結果を表示し、設定画面へ戻って再試行可能

## 改善

- **書き出し進捗ラベル**: `42% 完了 · 残り約 30秒` 形式で表示
- **一括書き出し**: 章 ZIP 書き出しでも同様のエラー・キャンセル UX を適用
- **E2E**: 全 45 シナリオ（書き出しエラー詳細・キャンセル回帰を追加）
- **ユニットテスト**: 184 件（`exportUx` を追加）

## 運用上の注意

- main への **連続 push は避け**、1 回の push ごとに Deploy ワークフローの完了（build → deploy → smoke）を待ってから次を push してください

---

# v1.7.0

映像↔画像のクロス差し替え MVP リリースです。スライドショー修正などで、クリップ種別を変えずに素材だけ差し替えるワークフローが可能になりました。

デモ: https://shunsukesaito00.github.io/mvmake/

## 追加

- **映像↔画像のメディア差し替え (MVP)**: インスペクターから動画クリップに画像を、画像クリップに動画を差し替え可能
- **差し替え UI 改善**: 映像・画像メディアを一覧表示し、クロス差し替え時は「へ変換」ラベルを表示

## 改善

- **動画メタデータ**: `Infinity` / 非有限の duration をフォールバック（タイムラインクラッシュ防止）
- **E2E**: 全 44 シナリオ（映像↔画像クロス差し替え 2 件を追加）
- **ユニットテスト**: 175 件（`buildCrossVisualClip`・クロス差し替えストアを追加）

## 運用上の注意

- main への **連続 push は避け**、1 回の push ごとに Deploy ワークフローの完了（build → deploy → smoke）を待ってから次を push してください

---

# v1.6.1

取り込み UX と大容量 IndexedDB 対策の安定化パッチです（ロードマップ上の v1.5.2 項目を v1.6.0 後に統合リリース）。

デモ: https://shunsukesaito00.github.io/mvmake/

## 追加

- **メディア取り込み進捗表示**: ファイル単位の進捗バーとファイル名表示
- **サムネイル/波形生成プレースホルダ**: enrich 中に「サムネ生成中」「波形生成中」を表示
- **自動保存インジケータ**: ツールバーに保存待ち / 保存中 / 保存済み / 失敗を表示
- **ブラウザ保存容量表示**: メディアパネルフッターに IndexedDB 使用率を表示（対応ブラウザ）

## 改善

- **自動保存**: メディア件数ごとの進捗更新、大容量プロジェクト時の通知、QuotaExceeded 時の詳細エラーメッセージ
- **IndexedDB 保存**: 大きな Blob 保存時にメインスレッドへ yield
- **E2E**: 全 42 シナリオ（複数ファイル取り込み・自動保存インジケータを追加）
- **ユニットテスト**: 170 件（`storageUtils`・`formatBytes`・取り込み進捗を追加）

## 運用上の注意

- main への **連続 push は避け**、1 回の push ごとに Deploy ワークフローの完了（build → deploy → smoke）を待ってから次を push してください

---

# v1.6.0

字幕ワークフローの往復を完成させる拡張リリースです。タイムライン上のテキストクリップから SRT / VTT をダウンロードできます。

デモ: https://shunsukesaito00.github.io/mvmake/

## 追加

- **SRT/VTT 字幕エクスポート (MVP)**: テキストクリップを開始時刻順に集約し、`.srt` / `.vtt` ファイルとしてダウンロード
- **字幕エクスポート UI**: テキストタブに「SRT を保存」「VTT を保存」ボタンを追加（クリップ件数表示付き）

## 改善

- **E2E**: 全 40 シナリオ（SRT エクスポート回帰テストを追加）
- **ユニットテスト**: 164 件（`srtExporter`・SRT 往復変換を追加）

## 運用上の注意

- main への **連続 push は避け**、1 回の push ごとに Deploy ワークフローの完了（build → deploy → smoke）を待ってから次を push してください

---

# v1.5.1

メディア取り込みの安定化パッチリリースです。数 MB 規模の動画インポート時に UI がフリーズする問題を修正しました。

デモ: https://shunsukesaito00.github.io/mvmake/

## 修正

- **メディア取り込み UI フリーズ**: 動画サムネイル生成を `onloadeddata` から `loadedmetadata` + `seeked` に変更し、1フレームのみ取得
- **非ブロッキング取り込み**: メタデータのみ先にリスト表示し、サムネイル・波形はバックグラウンドで後付け（`enrichMediaAsset` + `updateMediaAsset`）
- **波形デコード**: 先頭 5MB のみデコードしてメインスレッド負荷を軽減
- **タイムアウト**: 動画メタデータ・サムネイル生成に 8 秒タイムアウトを追加

## 改善

- **E2E**: 全 39 シナリオ（動画インポート回帰テストを追加）
- **ユニットテスト**: 152 件（`mediaLoader`・`updateMediaAsset` を追加）
- **ロードマップ**: 完成度見直し・修正計画（P0〜P2）を `FEATURE_COMPARISON.md` に追記

## 運用上の注意

- main への **連続 push は避け**、1 回の push ごとに Deploy ワークフローの完了（build → deploy → smoke）を待ってから次を push してください

---

# v1.5.0

入力・差し替え・字幕ワークフローの拡張リリースです。ブラウザ内録音から SRT 字幕インポートまで、結婚式ムービー制作の素材まわりが一段充実しました。

デモ: https://shunsukesaito00.github.io/mvmake/

## 追加

- **ブラウザ内ナレーション録音 (MVP)**: MediaRecorder でマイク入力を録音し、プレビュー後に BGM トラックへ AudioClip として配置
- **クリップのメディア差し替え (MVP)**: インスペクターから同種メディアへ差し替え。タイミング・長さを維持（短い素材はクランプ）
- **トランジション一括削除 (MVP)**: 効果タブから全映像トラックまたは選択トラックのトランジションを一括削除
- **SRT 字幕インポート (MVP)**: `.srt` ファイルを読み込み、字幕帯付きテキストクリップをタイムラインへ一括配置

## 改善

- **E2E**: 全 38 シナリオ（ナレーション録音・メディア差し替え・一括削除・SRT インポートを追加）
- **ユニットテスト**: 148 件（ナレーション録音・メディア差し替え・一括トランジション削除・SRT パーサーを追加）

## 運用上の注意

- main への **連続 push は避け**、1 回の push ごとに Deploy ワークフローの完了（build → deploy → smoke）を待ってから次を push してください

---

# v1.4.0

章の一括納品・フォント拡張・メディア整理・トランジション仕上げの拡張リリースです。結婚式ムービーの制作から書き出しまでの効率がさらに向上しました。

デモ: https://shunsukesaito00.github.io/mvmake/

## 追加

- **章マーカー一括書き出し (MVP)**: 全章区間を個別 MP4 化し ZIP でダウンロード
- **Google Fonts 追加 (MVP)**: Web Font 12 種を読み込み、インスペクター選択・書き出し前ロード
- **メディア一覧の検索・ソート (MVP)**: 名前検索・種類フィルタ（動画/画像/音声）・追加順/名前順ソート
- **隣接クリップへの一括トランジション (MVP)**: 効果タブから選択トラックまたは全映像トラックへ一括適用

## 改善

- **E2E**: 全 34 シナリオ（章一括 ZIP・Google Fonts・メディア検索ソート・一括トランジションを追加）
- **ユニットテスト**: 120 件（章一括書き出し・Google Fonts・メディア検索ソート・一括トランジションを追加）

## 運用上の注意

- main への **連続 push は避け**、1 回の push ごとに Deploy ワークフローの完了（build → deploy → smoke）を待ってから次を push してください

---

# v1.3.0

写真ガイド・章マーカー・テキスト表現の拡張リリースです。結婚式ムービーの区間編集とスタイル再利用がしやすくなりました。

デモ: https://shunsukesaito00.github.io/mvmake/

## 追加

- **写真ガイド連動スライドショー配置 (MVP)**: ガイドクリップ選択から区間内へ写真を一括配置、配置後はガイドを削除
- **テキスト背景・字幕帯 (MVP)**: `backgroundColor` / `padding` / `radius` をインスペクターで設定し compositor に描画
- **章マーカー区間の部分書き出し (MVP)**: 書き出しダイアログから章区間を In/Out に設定し、範囲 MP4 を書き出し
- **マーカー編集UI (MVP)**: 章マーカーのラベル・時刻をインスペクターで編集、タイムライン上ドラッグ移動
- **テキストスタイルのユーザー保存 (MVP)**: フォント・色・行間・字幕帯などの書式を localStorage に名前付き保存し、他クリップへ適用

## 改善

- **E2E**: 全 30 シナリオ（写真ガイド・字幕帯・章書き出し・マーカー編集・スタイル保存を追加）
- **ユニットテスト**: 98 件（章マーカー書き出し・マーカー編集・テキストスタイル保存を追加）

## 運用上の注意

- main への **連続 push は避け**、1 回の push ごとに Deploy ワークフローの完了（build → deploy → smoke）を待ってから次を push してください

---

# v1.2.0

SNS 向け縦型動画・構造化テンプレ・テキスト排版の拡張リリースです。結婚式ムービーの制作フローをさらに効率化しました。

デモ: https://shunsukesaito00.github.io/mvmake/

## 追加

- **縦型 9:16 プロジェクトプリセット (MVP)**: 1080×1920 を解像度プリセットに追加、書き出しラベル「9:16 で書き出し」対応
- **構造化ウェディングテンプレート拡張 (MVP)**: 「結婚式フル構成」テンプレートに章マーカー 5 件・写真ガイド 8 件を自動配置
- **テキスト行間・縦配置の詳細設定 (MVP)**: `lineHeight`（0.8〜2.5 倍）と `verticalAlign`（上/中央/下）をインスペクター・compositor に反映

## 改善

- **旧プロジェクト互換**: テキストクリップの `lineHeight` / `verticalAlign` は読み込み時にデフォルト値を補完
- **E2E**: 全 25 シナリオ（縦型書き出し・構造化テンプレ・行間/縦配置を追加）

## 運用上の注意

- main への **連続 push は避け**、1 回の push ごとに Deploy ワークフローの完了（build → deploy → smoke）を待ってから次を push してください

---

# v1.1.5

編集・書き出しの拡張リリースです。ルックプリセットから複数行テキストまで、結婚式ムービー制作の表現力を強化しました。

デモ: https://shunsukesaito00.github.io/mvmake/

## 追加

- **カラールックプリセット (MVP)**: ナチュラル / フィルム風 / セピア / ソフトなど 5 種をワンクリック適用
- **映像クリップのフェードイン/アウト (MVP)**: VideoClip / ImageClip の opacity を時間変化
- **プロジェクト解像度と書き出しの整合 (MVP)**: 4K / 正方形はネイティブ解像度で書き出し、720p はダウンスケール
- **テキスト複数行描画 (MVP)**: `\n` 改行をプレビュー・書き出し両方で描画

## 改善

- **書き出しプリセット**: 解像度を「プロジェクト / 720p」に整理（旧 1080p は互換正規化）
- **4K 書き出し**: 解像度に応じたビットレート自動調整
- **E2E**: 全 22 シナリオ（カラールック・映像フェード・正方形書き出し・複数行テキストを追加）

## 運用上の注意

- main への **連続 push は避け**、1 回の push ごとに Deploy ワークフローの完了（build → deploy → smoke）を待ってから次を push してください
